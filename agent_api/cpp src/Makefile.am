bin_PROGRAMS = agent_api
agent_api_SOURCES = agent_api.cpp mgmt_packet.cpp configuration.cpp

# set the include path found by configure
INCLUDES= $(all_includes)

# the library search path.
agent_api_LDFLAGS = $(all_libraries) 
